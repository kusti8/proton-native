import render from './render';

// Aliases for createElement method
const Text = 'TEXT';
const App = 'APP';
const Window = 'WINDOW';
const Button = 'BUTTON'
const VerticalBox = 'VERTICALBOX'

export { render, Text, App, Window, Button, VerticalBox };
