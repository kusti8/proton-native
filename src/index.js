import render from './render';
import { Box } from './react-components';

// Aliases for createElement method
const Text = 'TEXT';
const App = 'APP';
const Window = 'WINDOW';
const Button = 'BUTTON';
const VerticalBox = 'VERTICALBOX';
const HorizontalBox = 'HORIZONTALBOX';

export { render, Text, App, Window, Button, VerticalBox, HorizontalBox, Box };
